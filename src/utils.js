export const currentAgeForBirthYear = (birthYear) => {
    const currentYear = 2022 //as per codealong
    const currentAge = currentYear - birthYear

    return currentAge
}

// console.log(currentAgeForBirthYear(1984))
